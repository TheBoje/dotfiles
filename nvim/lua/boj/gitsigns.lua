require('gitsigns').setup {
    -- signs config
    -- display git blame message when `hover`
    current_line_blame = true,
    current_line_blame_opts = {
        virt_text_pos = 'right_align'
    }
}
